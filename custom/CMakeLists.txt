# STRATUM Custom Build Configuration
# NexamSystems

message(STATUS "STRATUM: Loading custom build configuration")

# Add custom branding resources if needed
# qt_add_resources(${CMAKE_PROJECT_NAME} "stratum_resources"
#     PREFIX "/custom"
#     FILES
#         branding/logos/Nx-logo.svg
#         branding/logos/stratum_512.png
# )

# Custom build completed
message(STATUS "STRATUM: Custom configuration loaded")
